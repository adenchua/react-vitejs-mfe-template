import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#3a3aff",
    },
    secondary: {
      main: "#00a3a3",
    },
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;
